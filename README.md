# Kana Confession Site (Ready for GH Pages)

Deployed automatically to: https://legittreew.github.io/confession-test

### Commands
```
npm install
npm start
npm run deploy
```

This will build and push your site to the `gh-pages` branch.
